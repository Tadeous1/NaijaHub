import { FormEvent, useEffect, useState } from 'react';
import { motion } from 'motion/react';
import { Search, MapPin, Briefcase, Filter, ArrowRight } from 'lucide-react';
import { Link, useSearchParams } from 'react-router-dom';
import { api, type Listing } from '../services/api';
import { cn } from '../lib/utils';

const levels = ['All', 'Entry Level', 'Mid Level', 'Senior Level'];
const types = ['All', 'Full-time', 'Part-time', 'Contract', 'Internship'];

export function Jobs() {
  const [searchParams, setSearchParams] = useSearchParams();
  const [search, setSearch] = useState(searchParams.get('q') || '');
  const [selectedType, setSelectedType] = useState(searchParams.get('type') || 'All');
  const [selectedExperience, setSelectedExperience] = useState(searchParams.get('experienceLevel') || 'All');
  const [selectedLocation, setSelectedLocation] = useState(searchParams.get('location') || 'All States');
  const [jobs, setJobs] = useState<Listing[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');

  useEffect(() => {
    setLoading(true);
    api.listings({ kind: 'job', q: search, type: selectedType, experienceLevel: selectedExperience, location: selectedLocation })
      .then(({ listings }) => setJobs(listings)).catch(err => setError(err.message)).finally(() => setLoading(false));
  }, [search, selectedType, selectedExperience, selectedLocation]);

  function submitSearch(event: FormEvent) { event.preventDefault(); setSearchParams({ ...(search ? { q: search } : {}), ...(selectedType !== 'All' ? { type: selectedType } : {}) }); }

  return <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12"><div className="mb-8 md:mb-12"><p className="text-xs font-bold uppercase tracking-[0.2em] text-emerald-600">Verified opportunity feed</p><h1 className="mt-2 text-3xl md:text-4xl font-bold text-gray-900 mb-4 tracking-tight text-center md:text-left">Find Your Next Job</h1><p className="text-gray-500 max-w-2xl text-base md:text-lg text-center md:text-left mx-auto md:mx-0">Search published listings from NaijaHub admins and approved external providers. Apply Now opens a tracked application flow before sending you to the official destination.</p></div><div className="grid grid-cols-1 lg:grid-cols-4 gap-8"><aside className="space-y-6"><div className="bg-white p-6 rounded-[2rem] border border-slate-200 shadow-sm"><h3 className="font-bold text-slate-800 mb-6 flex items-center gap-2"><Filter className="w-4 h-4" /> Refine Search</h3><div className="space-y-7"><div><label className="text-[10px] font-bold text-slate-400 uppercase tracking-[0.2em] mb-4 block">Experience Level</label><div className="space-y-1.5">{levels.map(level => <button key={level} onClick={() => setSelectedExperience(level)} className={cn('w-full text-left px-4 py-2.5 rounded-xl text-sm font-semibold transition-all', selectedExperience === level ? 'bg-emerald-50 text-emerald-700' : 'text-slate-600 hover:bg-slate-50')}>{level}</button>)}</div></div><div><label className="text-[10px] font-bold text-slate-400 uppercase tracking-[0.2em] mb-4 block">Job Type</label><div className="space-y-1.5">{types.map(type => <button key={type} onClick={() => setSelectedType(type)} className={cn('w-full text-left px-4 py-2.5 rounded-xl text-sm font-semibold transition-all', selectedType === type ? 'bg-emerald-50 text-emerald-700' : 'text-slate-600 hover:bg-slate-50')}>{type}</button>)}</div></div><div><label className="text-xs font-bold text-gray-400 uppercase tracking-widest mb-2 block">Location</label><select value={selectedLocation} onChange={e => setSelectedLocation(e.target.value)} className="w-full bg-gray-50 border-none rounded-lg text-sm p-3 focus:ring-2 focus:ring-emerald-500"><option>All States</option><option>Lagos</option><option>Abuja</option><option>Rivers</option><option>Kano</option><option>Remote</option></select></div></div></div><div className="bg-emerald-600 rounded-2xl p-6 text-white"><h4 className="font-bold mb-2">Need to publish an opening?</h4><p className="text-xs text-emerald-100 mb-4">Approved admins can post verified opportunities or import normalized provider records.</p><Link to="/post-a-listing" className="inline-flex w-full items-center justify-center rounded-xl bg-white py-3 text-sm font-bold text-emerald-700">Post a Listing <ArrowRight className="ml-2 h-4 w-4" /></Link></div></aside><div className="lg:col-span-3 space-y-6"><form onSubmit={submitSearch} className="relative"><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 w-5 h-5" /><input type="text" placeholder="Search by role, company, or keywords..." value={search} onChange={e => setSearch(e.target.value)} className="w-full pl-12 pr-28 py-4 bg-white border border-gray-100 rounded-2xl shadow-sm focus:outline-none focus:ring-2 focus:ring-emerald-500 font-medium" /><button className="absolute right-2 top-2 rounded-xl bg-emerald-600 px-5 py-2.5 font-bold text-white">Search</button></form>{error ? <p role="alert" className="rounded-xl bg-rose-50 px-4 py-3 font-semibold text-rose-700">{error}</p> : null}{loading ? <div className="rounded-3xl bg-white py-20 text-center text-slate-500">Loading published jobs…</div> : jobs.length ? <div className="space-y-4">{jobs.map(job => <motion.div key={job.id} initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className="bg-white p-6 rounded-[2rem] border border-slate-200 shadow-sm hover:border-emerald-200 transition-all group"><div className="flex flex-col md:flex-row md:items-center justify-between gap-4"><div className="flex gap-4"><div className="w-14 h-14 bg-slate-50 border border-slate-100 rounded-2xl flex items-center justify-center font-bold text-slate-800 text-xl flex-shrink-0 group-hover:scale-105 transition-transform">{job.organization?.[0]}</div><div><h3 className="text-lg font-bold text-slate-800 group-hover:text-emerald-600 transition-colors tracking-tight">{job.title}</h3><div className="flex flex-wrap items-center gap-4 text-[13px] text-slate-500 mt-1"><span className="flex items-center gap-1 font-medium"><Briefcase className="w-3.5 h-3.5" /> {job.organization}</span><span className="flex items-center gap-1 font-medium"><MapPin className="w-3.5 h-3.5" /> {job.location}</span><span className="bg-emerald-50 text-emerald-700 px-3 py-0.5 rounded-full text-[10px] font-bold uppercase tracking-wider">{job.type || 'Opportunity'}</span></div></div></div><div className="flex items-center gap-6"><div className="text-right hidden md:block"><div className="font-bold text-slate-900 tracking-tight">{job.salary || 'Not listed'}</div><div className="text-[10px] text-slate-400 font-bold uppercase tracking-widest mt-1">{job.sourceName}</div></div><Link to={`/apply/${job.id}`} className="bg-emerald-600 text-white px-8 py-3.5 rounded-full font-bold shadow-sm shadow-emerald-200 hover:bg-emerald-700 transition-all flex items-center gap-2">Apply Now <ArrowRight className="w-4 h-4" /></Link></div></div></motion.div>)}</div> : <div className="text-center py-20 bg-gray-50 rounded-3xl border-2 border-dashed border-gray-200"><Search className="w-12 h-12 text-gray-300 mx-auto mb-4" /><h3 className="text-xl font-bold text-gray-900 mb-1">No published jobs found</h3><p className="text-gray-500">Try another filter or ask an approved admin to publish a verified opportunity.</p></div>}</div></div></div>;
}
