import { useEffect, useMemo, useState } from 'react';
import { motion } from 'motion/react';
import { GraduationCap, Calendar, Award, ExternalLink, Search } from 'lucide-react';
import { Link } from 'react-router-dom';
import { api, type Listing } from '../services/api';
import { cn } from '../lib/utils';

export function Scholarships() {
  const [selectedCategory, setSelectedCategory] = useState('All');
  const [selectedField, setSelectedField] = useState('All');
  const [searchQuery, setSearchQuery] = useState('');
  const [scholarships, setScholarships] = useState<Listing[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');
  useEffect(() => { setLoading(true); api.listings({ kind: 'scholarship', q: searchQuery, category: selectedCategory }).then(({ listings }) => setScholarships(listings)).catch(err => setError(err.message)).finally(() => setLoading(false)); }, [searchQuery, selectedCategory]);
  const fields = useMemo(() => ['All', ...new Set(scholarships.flatMap(s => s.fieldOfStudy))], [scholarships]);
  const filtered = scholarships.filter(s => selectedField === 'All' || s.fieldOfStudy.includes(selectedField));
  return <div className="bg-gray-50 min-h-screen"><div className="bg-emerald-900 py-12 md:py-16"><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center"><motion.h1 initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} className="text-3xl sm:text-4xl md:text-5xl font-bold text-white mb-4 tracking-tight">Fund Your Future</motion.h1><p className="text-emerald-100 text-base md:text-lg max-w-2xl mx-auto opacity-80">Access published grants and scholarships from NaijaHub admins and approved external providers.</p></div></div><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 -mt-10 pb-20"><div className="max-w-3xl mx-auto mb-8"><div className="relative group"><Search className="absolute left-6 top-1/2 -translate-y-1/2 w-5 h-5 text-slate-400" /><input type="text" value={searchQuery} onChange={e => setSearchQuery(e.target.value)} placeholder="Search scholarship title or provider..." className="w-full bg-white border border-slate-200 rounded-[2rem] py-5 pl-16 pr-8 shadow-xl focus:ring-4 focus:ring-emerald-500/10 focus:border-emerald-500 outline-none text-slate-700 font-medium" /></div></div><div className="bg-white p-6 rounded-[2.5rem] shadow-xl border border-slate-200 mb-12 flex flex-col md:flex-row gap-8 items-center justify-between"><div className="flex flex-wrap items-center gap-2">{['All', 'Undergraduate', 'Postgraduate', 'Research', 'Short Course'].map(cat => <button key={cat} onClick={() => setSelectedCategory(cat)} className={cn('px-6 py-2.5 rounded-full text-sm font-bold transition-all', selectedCategory === cat ? 'bg-emerald-600 text-white shadow-md shadow-emerald-200' : 'bg-slate-50 text-slate-600 hover:bg-slate-100')}>{cat}</button>)}</div><div className="flex items-center gap-3 w-full md:w-auto"><label className="text-[10px] font-bold text-slate-400 uppercase tracking-widest whitespace-nowrap">Field:</label><select value={selectedField} onChange={e => setSelectedField(e.target.value)} className="bg-slate-50 border-none rounded-full px-6 py-2.5 text-sm font-bold text-slate-700 outline-none focus:ring-2 focus:ring-emerald-500 w-full">{fields.map(field => <option key={field}>{field}</option>)}</select></div></div>{error ? <p role="alert" className="mb-6 rounded-xl bg-rose-50 px-4 py-3 font-semibold text-rose-700">{error}</p> : null}{loading ? <div className="rounded-3xl bg-white py-20 text-center text-slate-500">Loading published scholarships…</div> : filtered.length ? <div className="grid grid-cols-1 md:grid-cols-2 gap-8">{filtered.map((s, i) => <motion.div key={s.id} initial={{ opacity: 0, scale: 0.95 }} animate={{ opacity: 1, scale: 1 }} transition={{ delay: i * 0.05 }} className="bg-white rounded-3xl p-8 shadow-xl shadow-gray-200/50 border border-transparent hover:border-emerald-200 transition-all flex flex-col"><div className="flex justify-between items-start mb-6"><div className="p-4 bg-emerald-50 rounded-2xl text-emerald-600"><GraduationCap className="w-8 h-8" /></div><span className="px-4 py-1.5 rounded-full text-xs font-bold uppercase tracking-widest bg-blue-100 text-blue-700">{s.coverage || 'Scholarship'}</span></div><h3 className="text-2xl font-bold text-gray-900 mb-2 leading-tight">{s.title}</h3><p className="text-gray-500 mb-6 line-clamp-2">{s.description}</p><div className="space-y-3 mb-8"><div className="flex items-center gap-3 text-sm text-gray-600"><Award className="w-4 h-4 text-emerald-500" /><span className="font-medium">{s.organization}</span></div><div className="flex items-center gap-3 text-sm text-gray-600"><Calendar className="w-4 h-4 text-emerald-500" /><span className="font-medium">Deadline: {s.deadline || 'Not listed'}</span></div></div><div className="mt-auto pt-6 border-t border-gray-50 flex items-center justify-between"><div className="text-xs font-bold text-gray-400 uppercase tracking-widest">{s.category || 'Opportunity'}</div><Link to={`/apply/${s.id}`} className="flex items-center gap-2 text-emerald-600 font-bold hover:gap-3 transition-all">Apply Now <ExternalLink className="w-4 h-4" /></Link></div></motion.div>)}</div> : <div className="text-center py-20"><Search className="w-16 h-16 text-gray-200 mx-auto mb-4" /><p className="text-gray-500 font-bold">No published scholarships found.</p></div>}</div></div>;
}
